绝对url：完整的url（6部分）；

相对url：省略了开头的若干部分；

$.get("/api/student")；


在页面中，使用相对url时，相对的是当前页面地址，也就是说url中省略的部分会以当前页面url地址进行补充。

相对url中（只写路径时）：
1，url如果以/开头，表示路径使用绝对路径，只相对当前页面的协议名、域名(IP)、端口号，路径部分使用相对url中的路径。
2，url如果非/开头，表示路径也使用相对路径，除了协议名、域名(IP)、端口号参照当前页面url，路径也会在当前页面url的路径上进行拼接。
